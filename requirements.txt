# FileSecureSuite v1.0.5 - Python Dependencies

# REQUIRED DEPENDENCIES
# These are essential for core encryption/decryption functionality
cryptography>=41.0.0

# OPTIONAL DEPENDENCIES
# These enhance features but are not required
# The application gracefully degrades if these are not installed
colorama>=0.4.6        # Terminal colors and styling (especially useful on Windows)
pyperclip>=1.8.2       # Clipboard copy/paste operations for sharing encrypted data
qrcode[pil]>=8.0       # QR code generation for key visualization
Pillow>=10.0.0         # Image processing library (required by qrcode[pil])

# PYTHON VERSION REQUIREMENT
# Requires: Python 3.8 or higher
